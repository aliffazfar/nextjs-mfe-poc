import React from 'react';

export default function Root(props) {
  return (
    <section>
      <h1>MFE1 is running!</h1>
      <p>This is a micro-frontend built with React and managed by single-spa.</p>
    </section>
  );
}